# 业务数据评测

分支 `cursor/eval-three-causes-2d90`，提交 `f1b927585c334bf839192ee3ab6d5f41bd914769`。工作区 `/Users/zxz/Documents/ai-project/fdex测试`。362 条串行重打（预览 API 并发 1，未开 362 轮聊天；上一刀 8 路并发串单已作废）。没有合进 main，没有推远程，没有过账。

闸看计划是不是这条结构。现网看右边报出的命中条数是不是等于业务库 `meta.count` 单独数出来的条数。右边没有报出总数的，算没对上。五类分开，不合成一个百分比。

生成规则：

- 已发布的边，每条走一遍。85 条。
- 每个对象上的每个枚举词，各加一遍。220 条。
- 更长的对象名盖住更短的对象名，每一对走一遍。6 对。
- 同一个枚举词落在多个对象上，每一组走一遍。29 组。
- 连接器字段指向了对象，但已发布的图上没有这条边。22 对。没有把所有「没有边」的对象对都做一遍。

`{{t("Departments")}}`、`{{t("Users")}}`、`{{t("Roles")}}` 是已发布的对象名，留在集合里。

## 分数

| 类 | 用例 | 闸 | 现网 |
|---|---:|---:|---:|
| 已发布的边 | 85 | 81 | 74 |
| 每个对象上的枚举词 | 220 | 220 | 213 |
| 长对象名盖住短对象名 | 6 | 6 | 6 |
| 同一个枚举词落在多个对象上 | 29 | 29 | 29 |
| 字段指向了对象，图上没有这条边 | 22 | 22 | 22 |

长名 6 对、图上没边的 22 对，闸和现网都对上了时下面不写该类。条数写「库里 / 右边」。

## 已发布的边

闸没过，条数也没对上（`2321ed1` 现网补拍）：

- 员工档案 → 员工档案（上级）。库里 80，现网「X的X」官方表落了下属 15，对侧 80 还没有 chip。
- 员工档案 → 员工档案（下属）。库里 15，现网官方表 **共 15 条**（`7f62dc2`，不是整表 120）。
- {{t("Departments")}} → {{t("Departments")}}（上级）。库里 11，右边整表 12。
- {{t("Departments")}} → {{t("Departments")}}（子级）。库里 4，右边整表 12。

闸过了，条数没对上：

- 采购订单 → 采购收货单。库里 386，右边 385。
- 采购订单 → 采购订单明细。库里 2088，右边 2085。
- 工单 → 工单处理记录。库里 800，右边 798。
- {{t("Departments")}} → {{t("Users")}}。库里 12，右边 0。
- {{t("Departments")}} → {{t("Users")}}。库里 12，右边 0。
- {{t("Roles")}} → {{t("Users")}}。库里 0，右边 14。
- {{t("Users")}} → {{t("Roles")}}。库里 0，右边 8。

## 每个对象上的枚举词

官方表仍是 `f1b9275` 串行 362：闸 220，现网 213。`2321ed1` 后这 7 条现网都对上了，官方 213 还没串行重打。

现网已对上：

- 工单 / 新建。库里 37，右边 37。
- 销售合同 / 销售合同。库里 220，右边 220。
- 销售合同 / 服务合同。库里 54，右边 54。
- 客户 / 企业客户。库里 212，右边 212。
- 客户 / 个人客户。库里 0，空表，页脚「共 0 条」。
- 客户 / 成交客户。库里 179，右边 179。
- 销售线索 / 新线索。库里 139，右边 139。

## 另外两类

长对象名盖住短对象名：6 条，闸 6，现网 6。

字段指向了对象、图上没有这条边：22 条，闸 22，现网 22。
