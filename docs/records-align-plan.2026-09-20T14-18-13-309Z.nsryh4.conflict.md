# 业务记录对齐 · 方案（待批）

依据：[对齐根因](records-align-root-cause.md)、[五问](records-panel-decisions.md) §4 §9、[项目上下文](project-context.md) 决策 15 / 17 / 18。  
不派人，直到你批准。一个 worker，**grok-4.6 high**，本机 scene-39，串行（RecordsPanel / sheet apply / 历史下拉）。不删文件。不写死对象、行数、单号。评测集仍后置。第 9 步写安全不夹带。

根因调查未在运行时打断点，实施时先印证「同一帧 `rows` / `paginatedRows` / tbody 节点」，再改。不要猜第三次。

## 这次是什么（已审）

[Find align and history](bc-a6b93c3b-60ea-5515-97cc-785ada956cc0)

- hop / 左 AI / chip / 横幅 / footer 都是 1。**不是** hop 没算出来。
- **不是** 空表闸、**不是** 1 行盖多行拒绝（代码里没有）、**不是**「返回」、**不是** 写预览抢表。
- 裂口：chrome 已按 pending 记成 1，**表体 children 没换成那一行**（旧同 kind 行还在，命中行在表底，单号重复）。最像 `paginatedRows` 握旧 slice，或 `sheetRowKey` 撞号导致旧 tr 没卸。
- 浮现历史：默认占位看不出当前是哪一次；没 session 时列出整个工作区；缓存没有就把条件丢掉；选中会被下一条 SSE 清掉；miss 只有灰条。

## 不做什么

- 不把全表推进 SQLite。
- 不用 `kind:` 最新一张冒充「那一次」。
- 不重做 hop 闸（G/H），除非复验暴露回归。
- 不报准确率、不出评测集。

## L. 一页只画同一张表

横幅、chip、footer、tbody 必须绑**同一份 row 数组**，禁止 chrome 走 lastPending、表体走另一份 memo / 旧 tr。

- `applySheet` 每次换新数组，禁止原地改短后再给 footer 读 `.length`。
- 行 key 不能只靠单号（同号会撞，旧行卸不掉）。用这次 sheet 身份 + 行身份。
- 新 fingerprint 整表替换 children，pager 回到第 1 页。
- 现查/改行/删除/过审/新建同一条。

## M. 浮现历史能看懂

规则不变：本会话 AI 浮现过的表；点一条换成那一次缓存；miss 只请人重新浮现。

- 下拉要显示**当前**是哪一条，不要永远停在占位。
- 每条 option 带 kind + 动作 + where/hop，不要只剩钟点。
- 没有 session 时不要倒出整个工作区。
- 人选了历史，SSE 不得偷偷清 pin、用最新 pending 冒充。
- miss：说清是哪一次（对象/动作/条件），请重新浮现；不要像控件坏了。
- 写预览先让开，再切历史。

## N. 原句再验

待审回款 ∩ 已到期合同：表体行数 = footer = chip = 左 AI = 交集命中，首行就是那一行。再点一条浮现历史，表必须换成那一次。失败就停。

## 请批

- **做 L+M+N**
- **只做 L+N**（表先对齐，历史另说）
- **先停**

## Ace 已批（2026-09-18）

**L+M+N。** 一个 worker，grok-4.6 high，串行。

## 第 1 次结果（2026-09-18，未完成）

[Align sheet then history](bc-a10dacf3-d200-5689-b29d-c6830aafb69f) 停在探针，**L/M/N 都没落地**。

- 产品仓仍是 `635787a`，没有验证文。
- 闸上注入 hop 后，探针里 tbody/footer/chip 一度是 1 行 PAY-2026-005。这不是现网 AI 过关。
- 浮现历史仍是占位、41 条、没 session 就倒工作区；option 条件是生字段名。
- 能看到的截图仍是旧的：表体多行，或历史 20 行对不上横幅。

## 下一步（待批）

按原 L+M+N **重写任务另起**一台，grok-4.6 high。先印证再改，改完必须现网原句 + 历史换表。失败就停。不闷声打补丁。

请批 **重派 L+M+N** 或 **先停**。

## Ace 已批（2026-09-18）

**重派 L+M+N。** 另起一台，grok-4.6 high。闸注入不算现网过关。

## 第 2 次结果（2026-09-18，图过、未进 main）

[Land align then prove](bc-ab6a9aa4-26f4-5572-9a4b-75a2d4692407)

- **N 图过。** 现网原句：左 AI 1 张 PAY-2026-005；右 tbody / footer / chip / 横幅都是 1，首行就是那一行。不是闸注入。图已审到。
- **M 图过。** 点「工单 · 改行」后表换成该缓存 1 行，下拉仍钉着。切到改行会再打开写预览抽屉（空变更）。
- 代码在旁支 `cursor/records-align-lmn-2407` @ `da77b81`，**不在 main**。未推 origin。

这还不是操作业务数据整块完成。评测集仍后置。

## 下一步（待批）

不派人，直到你批准。一个 worker，**grok-4.6 high**。不删文件。

**P. 落到 main**  
只把 `da77b81` 里通用的 L+M 拣进 `main`。先看 diff。不要探针。旁支不当日常树。

请批 **做 P** 或 **先停**。

## Ace 已批（2026-09-18）

**做 P。** 一个 worker，grok-4.6 high。只拣进 main，不推 origin。

## P 结果（2026-09-18）

[Land align onto main](bc-16e15e10-1de1-5c3a-bf7e-cb6689e4644a)

- **已对。** `main` 快进到 `da77b81`（`635787a` → ff-only）。三条 L+M：`bb0cf27` / `cfcf10a` / `da77b81`。探针未进仓。未推 origin。
- 未对：拣进后再走一遍现网 N（本步不做）。

## 第 9 步（Ace 已定）

等评测集出来之后再决定。现在不做。

## 下一步（待批）

操作业务数据还没整块完成。第 9 步和评测集都不做。建议下一刀只收浮现历史这条尾巴（你刚看到的空改行抽屉）。

**Q. 浮现历史收尾**  
一个 worker，grok-4.6 high，串行。不删文件。不写死对象。

- 点「改行 / 删除 / 过审」历史：表换成那一次缓存。没有可确认的变更时，不要弹出空抽屉。
- 每条 option 都能看出对象 + 动作 + 条件（有 speech 用原话，没有就用 where/hop 身份），不要只剩钟点。
- 只列本会话。写预览先让开再切。
- 现网：从当前 1 行回款切到一条现查历史、再切一条改行历史，表要对上，空抽屉不能冒出来。失败就停。

其它还在、先不动：模型仍可能拆单侧半表；孤立短名「没连业务」；操作记录 corpus / 旧回退徽章；连接器芯片和 kind 芯片混排。

请批例如：

- **做 Q**
- **先做模型不再拆半表**
- **先停**

## Ace 已批（2026-09-18）

**做 Q。** 一个 worker，grok-4.6 high。不做第 9 步。

## Q 结果（2026-09-18）

[Finish history picker UX](bc-ef69bf95-7ecc-5fbc-b013-332bf4201afd)

- **图过。** `main` `5afaa40`。从 1 行回款切到审批单现查 20 行，再切工单改行 1 行 `371713516372058`。表跟缓存，下拉钉住，**空改行抽屉没出现**。只列本会话。未推 origin。
- 仍差：没有 speech/where/hop 的写操作，option 第三段是空的（「工单 · 改行」）。

## Ace 已批（2026-09-18）：把还差的做完

第 9 步、评测集、审查 corpus 去留、连接器芯片混排：不动。

一个 worker，grok-4.6 high，串行。失败就停。先 R 再 S 再 U。R 过了再往下。

**R. 历史 option 第三段**  
没有 speech/where/hop 时，第三段用该次缓存的行身份（单号/主键），不要空白，也不要用钟点凑。

**S. 孤立短名**  
对不上连接器的孤立 kind 不能当预览/写目标；口语短名仍绑到已连接的那个 kind。对照只来自词表+图+连接器目录。  
**Ace 2026-09-18：** 不能影响模糊/口语查询。先解析到已连接 kind，再拒绝「解析结果是未连接孤立对象」。禁止短名黑名单，禁止要求念完全称。字段模糊搜不动。

**U. 整句一次 hop**  
同一句话多个相关对象，现网必须一次 preview 带 hop。模型拆两次单侧再 Bash 不算过关。不写死对象名。

现网复验写 store：`internal/verify-records-remaining.md`，图 `media/records-history-switched.png`（R）和需要时覆盖 `media/records-hop-intersection.png`（U）。

## R+S+U 结果（2026-09-18）

[Finish remaining records gaps](bc-b5d41717-09cc-5962-9f24-c4a160c1e3ae)

- **R 图过。** `main` `99deed0`。下拉「工单 · 改行 · 371713516372058」，表首行同一号。
- **S 闸过。** `main` `4555a37`。解析后 leftover 不在连接器目录则拒；口语短名仍绑已连接 kind。现网 leftover `线索` → `NO_CONNECTOR`。无短名黑名单。
- **U 图过。** 原句左 AI 1 张 PAY-2026-005，右表/chip/footer 1 行同一号。轨迹没逐条点开。
- 第 9 步、评测集、审查 corpus、连接器芯片：仍不动。未推 origin。

## 下一步（第一性原理，待批）

评测集、第 9 步、打包都不做。现查原句过了，不代表写也能用。

**V. 写动作现网走同一条 hop**  
一个 worker，grok-4.6 high，串行。不删文件。不写死对象。不静默 `biz_write`。

改行 / 删除 / 过审 / 新建（词表 `can` 有的才走）必须跟现查同一条关系链交集，预览是那一行，不能摊半表。人确认之前不写库。失败就停。

请批 **做 V**、**先做操作记录（corpus / 回退）**，或 **先停**。

## Ace 已批（2026-09-18）

**做 V。** 一个 worker，grok-4.6 high。只 preview，不静默写库。

## V 结果（2026-09-18，没过关）

[Prove live write hops](bc-60e67243-e94a-572a-a46e-02081fba4ed0)

- **改行现网过了。** 原句同一条关系链：左 AI 1 行，右 tbody / chip / footer 1 行，pending `from` 走图上的父型，抽屉是改行。未过账。
- **删除没过。** 左 AI 出了删除预览（同一行、令牌未用、未 `biz_write`）。右表仍是交集 1 行，**不是**半表；footer / 抽屉仍停在改行，「确认过账」还在。
- 过审 / 新建没走（失败即停）。
- `main` `08baa98`（hall 取消只对匹配的 `preview_id`；hall 已空不再 cancel）。未推 origin。第 9 步、评测集、审查 corpus：不动。

根因：同一会话里连续写预览会并进**同一个 opening**。下一跳令牌已经出来，pending / 抽屉的动作还握着上一跳。

这还不是操作业务数据整块完成。

## 下一步（待批）

不派人，直到你批准。一个 worker，**grok-4.6 high**，本机 scene-39，串行。不删文件。不写死对象、行数、单号。不静默 `biz_write`。不点确认过账。评测集、第 9 步、审查 corpus、打包都不做。

**W. 写预览换成最新那一次，再走完删除/过审/新建**

- 同一会话连续改行 → 删除 → 过审 → 新建（词表 `can` 有的才走）：pending、抽屉、footer 的动作必须跟**最新一次写预览**走，禁止并进同一个 opening 后停在上一跳。
- 取消只关这一次 `preview_id`。不得抹掉下一跳，也不得把表拉回别的会话缓存。
- 每一跳预览都是与现查同一条关系链的交集行，不能摊半表。
- 现网按这个顺序走完；哪一跳抽屉/pending 对不上动作，失败就停。人确认之前不写库。

请批 **做 W** 或 **先停**。

## Ace 已批（2026-09-18）

**做 W。** 一个 worker，grok-4.6 high。只 preview，不静默写库。

## W 结果（2026-09-18，图过）

[Switch latest write preview](bc-8767ed9a-53f1-50bc-bfec-26eba052b7e7)

- **图过。** 同一会话连续改行 → 删除 → 过审 → 新建：pending / 抽屉 / footer 都跟最新一跳。四跳都 hop 交集，不是半表。未过账。未静默 `biz_write`。
- 改行 / 删除 / 过审：交集 1 行、同一父型 hop。新建：预览是新单，抽屉「操作：新建」。
- `main` `8387d40`。取消只关该 `preview_id`：闸过，现网没点取消。未推 origin。
- 仍差：过审预览出现过「没有可展示的变更内容」（过审应标出改过的字段）。第 9 步、评测集、审查 corpus、决策 20/21：不动。

这还不是操作业务数据整块完成。

## 下一步（待批）

不派人，直到你批准。一个 worker，**grok-4.6 high**。不删文件。不写死对象。不静默写库。决策 20 / 21、第 9 步、评测集、打包都不做。

建议下一刀只收写预览这条尾巴：

**X. 现网取消再下一跳，过审预览要有变更字段**  
取消只关这一次；下一跳抽屉必须是新动作，表不能被拉回别的缓存。过审预览只展示改过的字段（原值→新值），不能空。

其它还在、先不动：审查 corpus 去留、旧回退徽章、连接器芯片混排。

请批 **做 X**、**先做操作记录（corpus / 回退）**，或 **先停**。

## Ace 已批（2026-09-18）

**做 X。** 一个 worker，grok-4.6 high。只 preview，不静默写库。

## 第 1 次结果（2026-09-18，未跑）

[Cancel then 过审 diffs](bc-f9856ba5-3e64-5c89-bcb2-5bb4e235ad5b) 限流打断。没有验证文，产品仓没动。

## Ace 已批（2026-09-18）

**重派 X。** 另起一台，grok-4.6 high。按原 X，不另发明。

## X 结果（2026-09-18，图过）

[Retry cancel 过审 diffs](bc-44993c0b-5a09-578e-bb21-6e996cad8b94)

- **图过。** 先改行再取消：只关那次令牌。下一跳过审，抽屉「操作：过审」，回款状态 待确认→已过。不是空变更。未过账。
- 取消后表曾回到本对象销售回款列表，**不是**别的会话缓存；过审又回到 hop 交集 1 行。
- `main` `1a28a96`。未推 origin。第 9 步、评测集、审查 corpus、决策 20/21：不动。

这还不是操作业务数据整块完成。

## 下一步（待批）

不派人，直到你批准。一个 worker，**grok-4.6 high**。不删文件。不写死对象。不静默写库。决策 20 / 21、第 9 步、评测集、打包都不做。不收 overlay（没另批）。

其它还在、先不动：审查 corpus 去留、旧回退徽章、连接器芯片混排、取消后先闪同对象列表。

请批 **先做操作记录（corpus / 回退）**，或 **先停**。

## Ace 已批（2026-09-18）

**做操作记录（corpus / 回退）。** 一个 worker，grok-4.6 high。不擅自拿掉「审查 corpus」。

**Y. 操作记录：corpus 原文 + 回退反馈**  
按 [五问](records-panel-decisions.md) §8：corpus 打开当时原文，不跳裸 JSON。回退用写入时的对象+主键；确认后立刻成败；成功标已回退；回退历史不得再长「可回退」。不静默写库。

## Y 结果（2026-09-18，图过）

[Fix corpus and rollback](bc-76159c26-9692-5155-9364-76a1ef0f770d)

- **图过。** 顶栏「已回退并写回。」列表铺满。回退条「已完成」，原改行「已回退」，没有再长「可回退」。抽屉「审查 corpus」仍在，打开「当时原文 / 改行工单」，不是裸 JSON。列表一眼能看到 原值→新值。
- `main` `7e6226a`。回退走确认，不是静默写。未推 origin。第 9 步、评测集、决策 20/21、overlay 收编：不动。

这还不是操作业务数据整块完成。

## 下一步（待批）

不派人，直到你批准。一个 worker，**grok-4.6 high**。不删文件。不写死对象。不静默写库。决策 20 / 21、第 9 步、评测集、打包、overlay 收编都不做。

其它还在、先不动：连接器芯片和 kind 芯片混排、取消后先闪同对象列表、审查 corpus 要不要留（现已能读原文）。

请批 **先做连接器芯片**、**先收取消后闪列表**，或 **先停**。

## Ace 已批（2026-09-19）

**先做连接器芯片、先收取消后闪列表。** 一个 worker，grok-4.6 high，串行。

**Z. 连接器芯片分开 + 取消后不闪目录**  
对象 chip 只反映当前这一次操作绑到的 kind。本地应用 / 连接器不和这次 kind chip 混成一排。取消写预览后表留在取消前那张，不先闪同对象全表。

## Ace 补需求（2026-09-19，Z 之后做）

**AA. 操作历史：搜索 + 颜色 + 翻页**  
按 [五问](records-panel-decisions.md) §11。Z 文件和操作记录列表会重叠，Z 过了再派，不插队。不写死对象。颜色走 token，不按业务状态字段上色。一页 20 条，翻页要停住。

等人批 **Z 过了做 AA**，或 **先停**。

## Ace 已批（2026-09-19）

**Z 过了做 AA。** 不插队。Z 图过后再派一台 grok-4.6 high。

## Z 结果（2026-09-19，图过）

[Separate chips fix flash](bc-cd5fff7c-5194-5d8f-a3fa-810fe2903127)

- **图过。** 连接器是下拉；kind chip 是这次的销售回款 / 销售合同，没有和「本地 · 供应商拜访台账」混排。取消改行后表仍是 1 行 PAY-2026-005。未过账。
- `main` `1358425`。未点「销售合同 20」，合同侧条数是不是这次 hop 命中未验。
- 未推 origin。第 9 步、评测集、决策 20/21：不动。

## AA 已派（2026-09-19）

按 §11：操作历史搜索、动作/状态颜色、一页 20 条翻页要停住。

## AA 结果（2026-09-19，图过）

[Search color page history](bc-c1124713-dfff-5289-be51-9795b2c2c557)

- **图过。** 搜「回退」14→5。回退 / 已完成分色。底栏一页 20，现网只有 14 条所以停在第 1/1 页。闸上 45 条翻到第 2 页能停住。未过账。
- `main` `46a44a2`。未推 origin。现网第 2 页没点成（条数不够，未写库凑）。点「销售合同 20」仍未验。

这还不是操作业务数据整块完成。

## 下一步（待批）

不派人，直到你批准。一个 worker，**grok-4.6 high**。不删文件。不写死对象。不静默写库。决策 20 / 21、第 9 步、评测集、打包、overlay 收编都不做。

请批 **先验销售合同 chip**、**先停**。

## Ace 已批（2026-09-19）

**先验销售合同 chip。** 一个 worker，grok-4.6 high。点这次操作非目标侧 kind chip，表必须是这次命中，不是目录。不对就按 §5 通用修再验。

## 合同侧 chip 结果（2026-09-19，图过）

[Prove hop kind chips](bc-9ec2818b-ce90-5118-9b44-d3ffa75249ba)

- **图过。** 合同侧 chip 从目录 20 收到这次命中 1。点开表 1 行 HT-2026-005。点回回款侧仍是 PAY-2026-005。未过账。
- `main` `85d3887`。未推 origin。第 9 步、评测集、决策 20/21、overlay 收编：不动。

这还不是操作业务数据整块完成。

## 下一步（待批）

不派人，直到你批准。决策 20 / 21、第 9 步、评测集、打包、审查 corpus 去留都不做。操作历史第 2 页、corpus 按钮去留：收益小，先不动。

按第一性原理（真可用、稳、少冗余），再抠业务记录 chip/历史收益已经小了。收益最大的是日常会炸的那条：

**AB. 复验并修切会话崩溃**  
当初切会话核心挂过（`22256b8`），重启后没再验。现网：切已有会话，5174/4318 不能断，业务记录和 AI 还在。不对就按通用原因修（会话身份 / 核心重载），禁止写死某一个 session。

若你愿意动结构（另批）：**收 overlay 成一份 lan-assist**。少一层拷贝和重载，升级不容易把补丁冲掉。比 AB 大、也更险，没另批不做。

**建议（2026-09-20，未批准；影响已补全）：** 方向上只留一份工作台自己跑的 lan-assist，不要维持 overlay→vendor 两份拷贝。不是现在卸 overlay 改用官方未打补丁的插件。不是并进 `~/.dsh`。先收口业务记录，再另批合闸。工作台影响见文末「合闸对工作台的影响」。

请批 **做 AB**、**先批 overlay 收编**，或 **先停**。

## Ace 纠正（2026-09-19）

先把**业务应用模块**做全、做好。切会话 AB、overlay 收编先放下。

收口方案：[业务应用模块收口](biz-app-module-plan.md)。下一刀是 **AC. 应用 Tab 一条龙**（描述→预览→采纳→SQLite 能增删改查）。

## Ace 已批（2026-09-19）

**先把业务记录整块做收口。** 创建应用主线已完成。下一刀 **BA**。不是评测集，不是第 9 步，不是 20/21。

## BA. 业务记录整块收口

一个 worker，**grok-4.6 high**。不删文件。不写死对象。不静默写。不杀 pnpm 5174。

五条必须同时现网：

1. 五个词表动作同一套 where/hop/bind；左栏和右表同一张 sheet。
2. 芯片只跟当次操作；连接器另选。
3. 预览只改过的字段；取消的不再弹出。
4. 浮现历史换当时那张表（session cache），不进操作记录库。
5. 切会话不炸。

决策 22：三 Tab、创建应用、拉伸 48/224 还在。评测集、第 9 步、20/21、overlay、corpus 去留不做。

Ace 2026-09-19 纠正：整块收口必须用下面 10 条活用例，不要只跑合同∩回款那一条。原句只当活用例，不写进产品。写操作只验预览，不点确认过账。

1. 停用客户还有哪些没关的工单？
2. 故障类而且紧急、还没关的工单是哪家客户的？
3. 待审回款挂在哪些合同上？把已到期的那些摊出来。
4. 把停用客户恒通改成成交。
5. 待审报销单都过一下。
6. 过一下请假单 LV-2026-018。
7. 把武汉云启那张紧急工单改成已解决。
8. 昆明精密机电还有哪些待审采购单？
9. 产假那张待审请假单是谁请的？过一下。
10. 东莞联创到期合同下还有哪些待审回款？把备注改成催收。

[Close records daily loop](bc-6e02db5e-a056-5087-b1cc-bac85cb3338b) 进行中。已转向这 10 条。

## BA 结果（2026-09-19，没过）

[Close records daily loop](bc-6e02db5e-a056-5087-b1cc-bac85cb3338b)

- **1 过：** 停用客户没关工单，右表工单 22，`TK20260623126`，chip 工单22/客户15。
- **2 过：** 故障紧急没关工单，右表工单 1，`TK20260105702`，chip 工单1/客户1。
- **3 没过：** 左栏已是已到期合同 ∩ 待审回款 `PAY-2026-005` / `HT-2026-005`；pending 也是回款 1 行；右表仍停在第 2 条工单，横幅还是第 2 条原话。
- **4–10 没跑。** 取消预览 / 浮现历史 / 切会话没跑。
- `main` `0e85357`（短名不当 leftover）。未推 origin。未过账。未静默写。
- 决策 22 顺带摸过，不算收口。

## Ace 已批（2026-09-19）

**继续做业务记录整块收口。** 下一刀 **BB**：先修右表换成当次 pending，再按 BA 那 10 条从第 1 条跑完。

## BB. 右表跟上 pending，再收口

一个 worker，**grok-4.6 high**。不删文件。不写死对象/单号。不静默写。不点确认过账。不杀 pnpm 5174。

先修：第 3 条右表必须换成当次 pending（不是第 2 条工单）。再测 10 条。取消预览、浮现历史、切会话都要跑。少一条算没收口。`0e85357` 留下。决策 22 还在。评测集、第 9 步、20/21 不做。

[Retry records close-out](bc-85c1f888-3d9f-5f35-8404-b5e41069c6b1) 进行中。

## BB 结果（2026-09-19，没过）

[Retry records close-out](bc-85c1f888-3d9f-5f35-8404-b5e41069c6b1)

- **1–2 过：** 与 BA 同，工单 22 / 工单 1。
- **3 过：** 右表已是销售回款 1 行 `PAY-2026-005`，横幅「AI 刚查了 销售回款 · 1 行」，不再停在第 2 条工单。
- **4 没过：** 「把停用客户恒通改成成交」改行抽屉没开。闸在两家恒通上提问，右表是客户 37，无 `preview_id`。
- **5–10、取消预览、浮现历史、切会话没跑。**
- 代码在旁支 `cursor/records-close-bb-c6b1` `1139b2e`，不在 daily `main`。未推 origin。未过账。
- `0e85357` leftover 仍在 main 历史里。

## Ace 已批（2026-09-20）

**继续做业务记录整块收口。** 下一刀 **BC**：BB 的右表换 pending 接到 main；第 4 条要走出改行预览，不要客户 37。再按 10 条跑完。

## BC. 恒通改行预览，再收口

一个 worker，**grok-4.6 high**。日常树 main。不写死恒通。匹配集=停用 ∩ 名称对得上；两家都可以两行预览。闸可以问哪家，右表不能倒成全表。写操作只验预览。少一条算没收口。

[Retry records close after 恒通](bc-59d2d612-ea68-552f-a3e2-49292cb116f4) 进行中。

## BC 结果（2026-09-20，没过）

[Retry records close after 恒通](bc-59d2d612-ea68-552f-a3e2-49292cb116f4)

- 工人自称 1–4 过、停在第 5 条（过审落到改行 0 行）。验证表和四格图对不上：第 3 格仍是工单 `TK20260105702`，表记 `chips-missing-related`。
- 日常树 `main`。未推 origin。未过账。
- 整块没收口。

## Ace 问：收口有没有写死，第 4 条重名怎么处理（2026-09-20）

明白：要的是产品代码有没有写死对象/名字，以及名称重复时到底怎么走，不是工人自报「hardcoded none」。

- **BA** 落到 main 的是短名 leftover（`0e85357`）：别的目录名以它结尾、且它不在图的 `from`/`to` 里，才当孤立短名。测是英文假名，没有工单/生产工单字面量。这解决的是对象短名，不是字段里两家同名。
- **BB / BC** 工人自报 `src/` 无恒通/武汉云启等活用例。探针和横幅里有那 10 句，不是产品。我没有再独立翻 BB/BC 产品 diff，自报不算审计。
- **第 4 条现网：** BA 没跑。BB 没有新的重名产品：闸问两家恒通哪家，右表倒成客户 37，无 `preview_id`。BC 自称改行预览 1 行 `CUST2056`，不是「停用 ∩ 名称对得上」的整集；整刀图对不上，第 4 条不算过。
- **该怎么处理（Ace 2026-09-20 纠正）：** 模糊名对上多家就停，人选完再继续。不是整集直接改行预览。人选的是这次命中行，不能倒目录。人要的本来就是一批（还有哪些 / 都过一下）仍出那批。不写死恒通。详见 [五问](records-panel-decisions.md) §12。

## Ace 已批（2026-09-20）

**继续业务记录收口。** 下一刀 **BD**。

## BD. 匹配集预览，再收口

一个 worker，**grok-4.6 high**。日常树 **main**。不写死对象/单号/恒通。不静默写。不点确认过账。不杀 pnpm 5174。不往 overlay 加新分叉。旁支不当日常树；BB/BC 还在旁支的 apply，先拣进 main 再测。

先修再按 BA 那 10 条从第 1 条跑完。少一条算没收口。图和自报不一致以图为准。

1. 第 3 条右表必须换成当次 pending（hop 回款交集），不能停在上一条工单。`shouldHoldSideKindView` 不得把新 pending 按旧 kind 按住。
2. 第 4 条（Ace 纠正）：模糊名对上多家必须停，让人从这次命中里选，选完再出改行预览。禁止默默挑一行，禁止整集直接改，禁止倒成客户全表。测这条时要真的选出一家再继续，不要写死选哪一家。人要的本来就是一批（第 1 / 5 条这类）不要按这条去一家家问。
3. 五动作同一套 where/hop/bind；左栏 = 右表。芯片只跟当次操作。
4. 预览只改过的字段；取消的不再弹出。
5. 浮现历史换当时 session cache，不进操作记录库。
6. 切会话不炸。
7. 决策 22：三 Tab、创建应用、拉伸 48/224、问 AI、走访应用还在。

验证写 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/internal/verify-records-close-bd.md`，十格图 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/media/records-close-bd.png`。

[Retry records matching-set close](bc-f089f80e-88cd-51a9-bf65-6b780479b420) 进行中。

评测集、第 9 步、20/21、overlay 收编、打包、corpus 去留不做。

## BD 结果（2026-09-20，没过）

[Retry records matching-set close](bc-f089f80e-88cd-51a9-bf65-6b780479b420)

- **1–3 图过：** 工单 22 `TK20260623126`；工单 1 `TK20260105702`；销售回款 1 `PAY-2026-005`，右表跟上 pending，不是上一条工单。
- **4 图过（先选再继续）：** 先停客户 2 行 `CUST2058` / `CUST2056`，chip 客户2，不是 37；左栏问哪一家；人选后才出改行预览 `CUST2056` 暂停合作→成交客户。未过账。左栏选择题选完还在。
- **5–10、浮现历史、切会话没跑。** 第 5 条被左栏未关选择题挡住。
- `main` `4700fe6`。未推 origin。
- 整块没收口。不自动重派。

## Ace 已批（2026-09-20）

**继续收口。** 下一刀 **BE**。

## BE. 收掉选择题，跑完 5–10

一个 worker，**grok-4.6 high**。日常树 **main**（`4700fe6` 留下）。不写死对象/单号/恒通。不静默写。不点确认过账。不杀 pnpm 5174。不往 overlay 加新分叉。

先修：人选完，左栏选择题必须关掉，下一句才能跑。然后从第 1 条把 BA 那 10 条跑完。1–4 不能回退（第 4 条仍是先停命中再选再预览；第 1 / 5 条仍是一批，不一家家问）。5–10、取消预览、浮现历史、切会话都要现网。少一条算没收口。图和自报不一致以图为准。决策 22 还在。

验证写 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/internal/verify-records-close-be.md`，十格图 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/media/records-close-be.png`。

[Finish records close remainder](bc-259bfdbd-9c31-5269-aee8-a3734d416e90) 进行中。

评测集、第 9 步、20/21、overlay 收编、打包、corpus 去留不做。

## BE 结果（2026-09-20，没过）

[Finish records close remainder](bc-259bfdbd-9c31-5269-aee8-a3734d416e90)

- **1 图过：** 工单 21 `TK20260623126`，chip 工单21 / 客户14。不是一家家问。BD 当时是 22/15。
- **2 没过：** 左栏 `biz_preview` NOT_FOUND 后问「你要看哪一档」；右表仍是第 1 条工单 21，不是 BD 那张 1 行 `TK20260105702`。第 2 条是一批现查，不该变成选择题。
- **3–10、取消、浮现历史、切会话、第 4 条人选后关题没跑。**
- 产品 `main` `aa20391`（人选后 `cancelAi`）现网没验到。未推 origin。未过账。
- 整块没收口。不自动重派。

## Ace 已批（2026-09-20）

**查第 2 条查不到的根因再修，并扫同类。** 下一刀 **BF**。不是整块 10 条重跑，除非现网证明同一条根因。

## BF. 现查沿用旧表 / 条件绑错

Ace 现网对照：

- [AI 报 0](/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/media/case-2-ai-zero.png)：原话「故障类而且紧急、还没关的工单是哪家客户的？」；现查结论没有；写「对照刚才那页未关工单，最高只到高，没有紧急」。
- [业务系统 8 行](/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/media/case-2-biz-eight.png)：故障 + 紧急，状态是已分派 / 处理中 / 新建，首行 `TK20260105702`。这些行在，不是空库。

先印证再改，不要猜第三次。按这个顺序查，查到就停在真因上修：

1. **下一句沿用上一张表。** 第 1 条是停用客户未关工单（那页最高只到高）。第 2 条必须自己按本句 where 现查全库，禁止拿上一张 pending / 停用客户条件当底表。BE 图右表仍停在工单 21，左栏也说「对照刚才那页」。
2. **「没关」绑错。** 已分派 / 处理中 / 新建必须能算未关。关闭态只来自该 kind 词表/schema 枚举，禁止写死已解决 / 已关闭 / processing。
3. **「紧急」「故障」绑错。** 口语绑到该字段的 schema 标题 + 枚举标签，禁止写死 incident / urgent。

同类必须一起扫（五动作同一条 bind，不写死第 2 条）：同一会话下一句是否夹带上一句的 where / kind / pending 底表；写预览 opening 是否并进旧 opening；右表是否按住旧 sheet。人要的是一批（还有哪些 / 哪家客户的 / 都过一下）不要变成选择题。模糊名对上多家仍按五问 §12 先选再继续。

一个 worker，**grok-4.6 high**。日常树 **main**。不写死故障 / 紧急 / 工单 / 单号。不静默写。不点确认过账。不杀 pnpm 5174。不往 overlay 加新分叉。

现网至少：同一会话先说第 1 条，再说第 2 条。第 2 条左栏和右表必须是这次条件的命中（含 `TK20260105702` 这类行，行数跟业务系统对得上），不能报 0，不能停在第 1 条那 21 行，不能出「哪一档」选择题。再抽一条同类（第 3 条或另一句新现查）证明没有夹带上一句条件。决策 22 要摸。失败就停。

验证写 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/internal/verify-records-query-miss-bf.md`，图 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/media/records-query-miss-bf.png`。

[Fix stale-sheet query miss](bc-5daf733e-77b4-50a6-ba8b-6bf33bbaa01d) 进行中。

评测集、第 9 步、20/21、overlay 收编、打包、corpus 去留、整块 10 条收口不做（除非同一根因必须带到）。

## BF 结果（2026-09-20，图过）

[Fix stale-sheet query miss](bc-5daf733e-77b4-50a6-ba8b-6bf33bbaa01d)

- **真因：** hop 从空 where 的客户目录页（20 条）起探，那 8 张单的客户不在这页 → `NOT_FOUND`；空 miss 按住旧表。不是沿用上一张 pending SQL，也不是「没关」绑错。
- **修：** 从第一个有 bind 的 hop 步起探，空祖先用子行 FK 回填。
- **2 图过：** 工单 8，含 `TK20260105702`，类型全故障、优先全紧急，chip 工单8/客户8。不是 0，不是第 1 条 21 行，没有「哪一档」。
- **3 图过：** 右表换成销售回款 1 `PAY-2026-005`，没夹带工单 8。
- `main` `98af488`。未推 origin。未过账。
- 整块 10 条没收口。不自动重派。

## Ace 已批（2026-09-20）

**继续收口。** 下一刀 **BG**。

## BG. 带上 BF，跑完 10 条

一个 worker，**grok-4.6 high**。日常树 **main**（`98af488` 留下）。不写死对象/单号/恒通/故障/紧急。不静默写。不点确认过账。不杀 pnpm 5174。不往 overlay 加新分叉。

从第 1 条把 BA 那 10 条跑完。少一条算没收口。图和自报不一致以图为准。

1. 1–3 不能回退：第 2 条仍是故障+紧急未关那批（约 8 行，含 `TK20260105702`），不是 0、不是第 1 条 21 行、不出「哪一档」。第 3 条右表换成当次 pending 回款，不夹带上一张工单。
2. 第 4 条按五问 §12：模糊名对上多家先停在命中行，人选完再出改行预览；人选完左栏选择题必须关掉。禁止倒全表，禁止默默挑一行。
3. 第 1 / 5 条是一批，不一家家问。
4. 5–10、取消预览、浮现历史、切会话都要现网。
5. 决策 22：三 Tab、创建应用、拉伸 48/224、问 AI、走访应用还在。

验证写 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/internal/verify-records-close-bg.md`，十格图 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/media/records-close-bg.png`。

[Finish ten-case records close](bc-e8b23ddd-6eb2-56cf-959d-4bbb493f8898) 进行中。

评测集、第 9 步、20/21、overlay 收编、打包、corpus 去留不做。

## BG 结果（2026-09-20，没过）

[Finish ten-case records close](bc-e8b23ddd-6eb2-56cf-959d-4bbb493f8898)

- **1–3 图过：** 相对 BF 没回退。工单 21；工单 8 含 `TK20260105702`；回款 `PAY-2026-005`。
- **4 图过（本趟只有一家）：** 停用∩恒通只剩 `CUST2058`，改行预览那一家，未过账。不是客户 37。没有两家先选（`CUST2056` 已是成交）。
- **5 没过：** 左栏说过待审报销 97 条，右表打到词表短名「报销单」0 行。
- **取消没过：** 抽屉关了，表倒成客户目录约 20 行，不是取消前那张预览表。
- **6–10、浮现历史、切会话没跑。**
- `main` `2d4bbac`（空 hop 侧视图不得按住有行 pending）。`98af488` 还在。未推 origin。
- 整块没收口。不自动重派。

## Ace 已批（2026-09-20）

**找收口总因并修管子，不要再空转 10 条。** 下一刀 **BH**。

## 收口总因（2026-09-20，整条，不再追加）

全文：[收口总因](records-close-root-cause.md)。后面只按那份修，不再说一层补一层印证。

一句话：工作台把「闸已经查对的那一次」和「人看见的 / 模型下一步打的」拆成了两套。插件没有这套拆分。

## 收口总因（旧稿，以 records-close-root-cause.md 为准）

不是「再跑一遍 10 条」。BA–BG 死法不同，管子是同一根：

**一句话必须收成：已连接 kind + 这一次 where/hop + 一张 pending；右表按这张表换上。** 解析落空、短名落空、空 hop 只探目录第 1 页、空表、取消，都不得按住旧表或倒目录。

已修过的洞：短名 leftover（`0e85357`）、空 hop 客户目录第 1 页（BF `98af488`）、右表按住旧 kind（BB/BG apply）。

还没收住的洞（BG 现网，已纠正猜测）：

词表**不是缺费用报销**。词表和图里同时挂了「费用报销」和「报销单」两个 kind。现网先按费用报销查到 97 条待审，过审卡住后模型改打 `kind=报销单`，空表盖上去。leftover 只认「长名以短名结尾」，费用报销对不上报销单。取消倒客户目录是 apply 另一条，和短名不是同一句代码。

**更深一层（Ace 2026-09-20：双 kind 还不是根）：**

决策 15 规定：对象来自已连接系统的表；词表口语是别名；图是表和表的关系。

现网反了。catalog / `kindsFromGraphNodes` 从图谱 `skos:Concept` 出 kind，不是从连接器的表出 kind。图谱里多出来的概念没有对应业务表，也能进 catalog、也能 preview。闸把 catalog 里任意 kind 当真表打。费用报销过审先失败，模型才改打这个假 kind。leftover 只处理「长名以短名结尾」，救不了两个并列概念。

所以「词表里有两个名字」是果。根是：**谁有资格当 kind 搞错了。**

这三问已钉在 [收口总因](records-close-root-cause.md)，不再当印证清单：报销单是图谱并列概念不是当前表生成的；过审两枪没出令牌后模型改打空 kind；取消倒目录是 apply 回潮。后面只按那份四条契约修。

### 为什么插件 10 条过、工作台不过（不是 overlay）

插件：一句话 → 闸打一次 preview → 人看会话里那一次结果。没有第二张表，没有第二次 preview。

工作台多了三截，和补丁准不准无关：

1. **BFF 会再写 pending。** 闸 `/state` 被 1s 轮询、`GET pending-sheet`、`POST /preview` 三路写。插件只有模型那一次工具调用。
2. **右表自己有闸。** 空表拒绝、按住旧 kind、取消回目录。左栏可以已经是 97 条费用报销，右表画出报销单 0 或客户 20。插件没有这张表。
3. **模型会看着右表再打一枪。** 费用报销过审先失败或右表空了，会话里再改打 `kind=报销单`。插件里人看见 97 条预览就停。

不是插件能算、工作台算不准。是工作台多了「第二张表 + 第二次 preview」，把已经查对的结果盖掉或按住。BH 印证必须对上这三截，不许用 overlay 当原因。

### 为什么插件 10 条过、工作台不过（不是 overlay）

插件：一句话 → 闸打一次 preview → 人看会话里那一次结果。没有第二张表，没有第二次 preview。

工作台多了三截，和补丁准不准无关：

1. **BFF 会再写 pending。** 闸 `/state` 被 1s 轮询、`GET pending-sheet`、`POST /preview` 三路写。插件只有模型那一次工具调用。
2. **右表自己有闸。** 空表拒绝、按住旧 kind、取消回目录。左栏可以已经是 97 条费用报销，右表画出报销单 0 或客户 20。插件没有这张表，所以 Ace 在插件里看不到「查错了」。
3. **模型会看着右表再打一枪。** 费用报销过审先失败或右表空了，会话里再改打 `kind=报销单`。插件里人看见 97 条预览就停。工作台把第二次空结果盖上去。

所以不是插件能算、工作台算不准。是工作台多了「第二张表 + 第二次 preview」，把已经查对的结果盖掉或按住。

BH 印证时必须对上这三截，不许用 overlay 当原因。

五动作同一条 bind。模糊名多家仍按五问 §12 先选再继续。人要的是一批，不要变成选择题。

## BH. 短名收到已连接 kind，取消不倒目录

一个 worker，**grok-4.6 high**。日常树 **main**。三问已钉，不要再追加印证。不写死报销单/费用报销/请假单/恒通/单号。不静默写。不点确认过账。不杀 pnpm 5174。不往 overlay 加新分叉。不要把整块 10 条当本刀过关标准。

现网必须：

1. 「待审报销单都过一下」只验预览：kind 是已连接的那张报销表（现网是费用报销这类），右表是待审那一批，不是「报销单」0 行。左栏和右表同一张。
2. 取消该预览：抽屉关上，表仍是刚才那批，不是客户目录。
3. 再抽一句同形短名现查或过审（如请假单），证明不是为报销单单写。
4. 决策 22 要摸。失败就停。

验证写 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/internal/verify-records-bind-pipe-bh.md`，图 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/media/records-bind-pipe-bh.png`。真因写进验证文。

评测集、第 9 步、20/21、overlay 收编、打包、corpus 去留、整块 10 条不做。

[Fix spoken-kind bind pipe](bc-868bf743-0b36-56fe-ad61-c7a4767a96ba) 进行中。

## BH 结果（2026-09-20，没过）

[Fix spoken-kind bind pipe](bc-868bf743-0b36-56fe-ad61-c7a4767a96ba)

- **图对：** POST 现查「待审报销一批」右表费用报销 97，首行 `EXP20251224598`；改行取消后仍是这 97，不是客户目录 20。图 `media/records-bind-pipe-bh.png`。
- **没过：** 代码在旁支 `cursor/records-bind-pipe-bh-96ba` `8d6266c`，不在 daily `main`。契约要的「待审报销单都过一下」过审预览没走出来；他们打的过审被 leftover 吃成「对不上一批」502，没有 `preview_id`。左栏是空输入框，不是那句话。请假单 20 只有探针，2-up 图里没有。
- 未过账。未推 origin。整块 10 条按约定没跑。
- 不自动重派。

## Ace 已批（2026-09-20）

**继续做。** 不要偷懒。决策 22。下一刀 **BI**。

## BI. 拣进 main，现网走出过审一批

一个 worker，**grok-4.6 high**。日常树 **main**。先看旁支 `cursor/records-bind-pipe-bh-96ba` `8d6266c` 的 diff，只拣通用契约。旁支不当日常。不写死报销单/费用报销/请假单/恒通/单号。不静默写。不点确认过账。不杀 pnpm 5174。不往 overlay 加新分叉；leftover 若把「一批 / 都」吃成对不上，只收「动作/批量词不当 kind」，禁止对照表。不要空转 10 条。不要追加印证。按 [收口总因](records-close-root-cause.md) 四条。

现网必须（左栏说话，禁止只 POST 现查当过关）：

1. 「待审报销单都过一下」走出已连接报销表的待审**过审**预览，右表同一批，不是报销单 0，不是只现查 97。
2. 取消后仍是那批，不是客户目录。
3. 同形短名仍通用。
4. 决策 22：1–3 已过的 hop、取消、三 Tab、创建、拉伸 48/224、问 AI、走访还在。

验证写 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/internal/verify-records-bind-pipe-bi.md`，图 `/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/media/records-bind-pipe-bi.png`。图和自报不一致以图为准。

评测集、第 9 步、20/21、overlay 收编、打包、corpus 去留、整块 10 条不做。

[Land bind pipe on main](bc-06b75c78-8054-517d-b564-725c03e5d708) 进行中。

## BI 结果（2026-09-20，`455e6e5` 自报，leftover 未收）

[Land bind pipe on main](bc-06b75c78-8054-517d-b564-725c03e5d708)

- 已对：待审报销过审预览 pending 费用报销 97 `EXP20251224598` 有令牌；取消后仍 97，不是客户目录。
- 仍差（当时）：闸仍接受 `kind=报销单` 打出 97；芯片/横幅口语名「报销单」；短名取消图是请假申请 210；hop 工单有时被挤成审批单/请假申请。
- 日常树 `main` `455e6e5`。未推 origin。整块 10 条没跑。

Ace 已批继续收口总因 leftover，不重开 BI，不追加第五条契约。

## BJ. leftover：连接表、一张 pending、右表这一张

按 [收口总因](records-close-root-cause.md) 四条，只收 BI 之后还开着的：

1. 只有连着业务表的对象能预览/写。口语短名走 型槽+图 收到已连接表。空资源 kind 不能预览。
2. 一句话一张 pending。
3. 右表换上这一张。空表 / 假 kind / 取消不得按住旧表或倒目录。
4. 模糊多家先选再继续（不重开）。

hop 说工单必须是工单，不是审批单。不写死报销单/请假单/工单对照表。不 `(in graph)` 产品判断。日常树 **main**。不杀 pnpm 5174。

## BJ 结果（2026-09-20）

[Finish records close contract](bc-65938102-285e-52d8-8c72-e577d75077f2)

母体：决策 15 对象=已连接表；闸校验 structured kind，不解析人话。

| 格 | 状态 | 母体 / fdex 或实测 |
|---|---|---|
| 1 连接表/别名/空资源 | 已对 | 母体 records-close-root-cause §上游；fdex `resolveConnectedKindName` + POST `GraphOnlyNoTable` → `该对象没有连接业务表`；`kind=报销单` → 费用报销 97；kinds 40 含工单/客户 |
| 2 一句话一张 pending | 已对 | hop 原句 pending 工单 21 `TK20260623126`，再读同一张 |
| 3 右表+取消不倒目录 | 已对 | hop footer 共 21 / chip 工单21 客户14；空资源盖不掉；改行预览取消后仍工单 21 现查、speech 仍是停用客户那句，不是工单 22 目录 |
| 4 模糊多家先选再继续 | 已对（不重开） | 五问 §12 |
| hop 工单不是审批单 | 已对 | pending.kind=工单，chips 工单21/客户14，不是请假申请/审批单 |
| 决策 22 三 Tab / 走访 / 抄表 | 已摸过 | 应用/业务记录/操作记录；走访 `updatedAt=2026-09-19T06:03:58.932Z` 未变；抄表还在。拉伸 48/224 本刀未测 |

- `main` `5721789`（其上 `bb12a33` 连接表/别名）。闸注入=no。未静默 `biz_write`。未点确认过账。未推 origin。未杀 pnpm 5174。
- 图 `media/records-close-contract.png`（hop / 改行预览 / 取消后）。验证 `internal/verify-records-close-contract.md`。
- 整块 10 条没收口。评测集 / 第 9 步 / 20/21 / overlay 收编 / 打包不做。

已对：连接表预览、工单 hop 一张 pending、取消仍 21
未对：拉伸 48/224
仍差：整块 10 条没跑

### 三种做法对工作台

**维持 overlay（现状）**  
已经能用的模块不会因为「维持」而变。代价是两份文件会漂：工人改 overlay 忘了拷 vendor，你看到的还是旧闸。BA/BB/BC 没过的主因在右表 apply / BFF，不在这一层。收口期间不要再往 overlay 加新分叉。

**现在改用官方未打补丁的插件**  
会掉跨对象 hop、词表 `can`、短名 leftover、写预览 opening、过审只标改过的字段。IM 和过账共一个进程，换错整棵树 IM 也可能一起挂。若顺带改回 `x-dsh-cwd`，中文工作区会再 NO_VOCAB。若指到 `~/.dsh`，会碰到你说正常的那份官方插件和会话，两边互相污染。10 条收口更没法验。不做。

**收口之后另批：工作台只跑一份带补丁的闸，废掉二次拷贝**  
家仍是 `~/.dsh-fde-x`，官方 `~/.dsh` 不动。合完行为应等于现在「拷成功之后」，少的是拷贝步骤。不修右表 apply，第 3 条右表停旧工单那种问题还在。升级官方 lan-assist 变成我们自己拣补丁，不是自动跟上。

### 按模块：会动 / 不动 / 会坏

- **业务记录：** 闸上的 hop / `can` / leftover / 写 hop 在 overlay 里。合进工作台自己那份并留补丁，行为应不变。换成官方未打补丁会坏。右表跟不上 pending、客户整表倾倒，是 apply，合闸修不了。
- **操作记录：** traces / 回退走同一份闸。合错树，回退预览和「只记改过的字段」会回潮。
- **IM：** 和过账共进程。合闸必须只动 `dsh-lan-assist`，禁止顺手刷新整个 vendor（会碰到 semantic-os）。合完要复验拟回 / 未读 / 不自动发。
- **过账：** 人审闸不能丢。V/W/X 的 opening（取消改行不得抹掉下一条删除预览）在 `gate.js` 补丁里，合的时候必须带走。
- **问 AI / 左栏：** 引擎是 DSH，不是闸。合闸要重载插件时，会话不能断。BFF 翻译意图仍在，合闸不等于改回插件手感。
- **记忆：** semantic-os，另一份插件，同一家 `~/.dsh-fde-x`。合闸脚本不得覆盖它。门禁仍看 DSH 家会话 cwd。
- **早报：** IM / 业务块读闸的 `/state` 和 preview。`/state` 形状变了，这两块会各自报错，其它块按设计不该陪葬。
- **创建应用：** L1 + SQLite，不经闸。建造里已不带业务 lookup。合闸不应改工作面。应用里跳进 IM / 业务记录，跟的是那两个模块合完后的状态。
- **浮窗 / 拉伸 / 会话列表：** 不是闸。合闸不得改 Stage / 左栏。决策 22 仍要复验拉伸 48/224，因为重载核心曾经搞坏过。
- **连接器 / 词表+图：** catalog 和从 schema 生成关系走闸。换新系统仍要现场生成，禁止拷你的工单图。合进官方未打补丁，这条生成能力可能不在。
- **中文 cwd：** 在 BFF / `dsh-core`，不在 overlay 文件本身。合闸若把请求改回 `x-dsh-cwd`，`fdex测试` 会再挂。
- **打包 / 首启：** 决策 14 仍停。合闸之后安装包应打一份带补丁的 `plugins/dsh-lan-assist`，首启不再 overlay 再拷一层。现在合闸不会让打包突然可做。
- **4318 / 4319：** 还在。改正在跑的 vendor 要重载闸，不要杀 `pnpm dev`。

### 合的时候决策 22 必复验

五动作只验预览、左栏=右表、当次芯片、浮现历史、操作记录回退、IM、问 AI、拉伸 48/224、中文 cwd 业务记录。创建应用主线、记忆起草点头，路径没改也要摸一下没坏。

### 仍主张的顺序（未批准）

1. 现在继续业务记录收口，不再往 overlay 加新分叉。
2. 收口之后另批「废掉 vendor 二次拷贝，只留工作台自己那份带补丁的闸」。
3. 合的时候按上面矩阵复验。
4. BFF / 右表 apply 单独排，不要卖成「改回插件」。
