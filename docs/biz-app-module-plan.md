# 业务应用模块收口（待批）

依据：[规格 04](specs/04-app-spec-runtime.md)、[规格 05](specs/05-business-records.md)、[项目上下文](project-context.md) 决策 1 / 2 / 3 / 15。  
切会话（AB）、overlay 收编、决策 20 / 21、第 9 步、评测集、打包：先放下。

业务应用就三页：**应用**、**业务记录**、**操作记录**。不新增路由，不改顶栏。

## 现在到哪

**业务记录 / 操作记录：** 主线已现网图过（现查 hop、写预览、取消、chip、回退、历史搜索）。还差的是另批项和小尾巴，先不动。

**应用：** 这才是模块没做全的洞。规格 04 要的是：描述需求 → AI 出 spec → 预览 → 人采纳并激活 → SQLite 建表 → 表格/表单能增删改查。现网已经有本地草稿应用，但不能当成已经「做好」。Wave 4 应用栈当时没走现网验收。

## 不做什么

- 不做 L2 代码应用、日历视图、跨应用关联、多用户。
- 不把业务系统行抄进 SQLite。引用业务数据只存 id + 快照，写外部走预览确认。
- 不写死某一个台账名字。现有「供应商拜访台账」只是用例。
- 不重做业务记录 / 操作记录，除非做应用时踩回归。

## AC. 应用 Tab 一条龙现网能用

一个 worker，**grok-4.6 high**，本机 scene-39，串行。不删文件。

先查现网应用 Tab 已经到哪（创建、预览、采纳、表格），缺哪补哪，禁止整页重写。

过关（图里必须能读到）：

1. 在应用 Tab 用 AI 创建一个本地应用（描述来自你的话，不写死名字）。
2. 预览之后能采纳并激活。
3. 激活后表格能新建行，刷新还在（SQLite 真值）。
4. 这个应用出现在应用列表，不跑到业务记录 kind chip 里冒充业务表。

失败就停。人确认之前不写业务系统。

## 请批

- **做 AC**
- **先停**

## Ace 已批（2026-09-19）

**做 AC。** 一个 worker，grok-4.6 high。先查现网再补，不整页重写。

## AC 结果（2026-09-19，图过）

[Ship app tab end-to-end](bc-8aef5f9c-8e51-558a-8b23-f31883a365ff)

- **图过。** 现网描述生成「本周现场走访记录」→ 预览 → 采纳激活（运行中）→ 新建 1 行，刷新还在。应用在「我的业务应用」，没进业务记录 kind chip。未写业务系统。
- `main` `345d570`。未推 origin。名字只是现网用例，没写进产品代码。

应用一条龙过了，业务应用模块还没做全。规格 04 后半（看板拖卡、改 spec 加字段、版本回滚）没验。

## 下一步（待批）

不派人，直到你批准。一个 worker，**grok-4.6 high**。不删文件。不写死应用名。不 restyle 别的模块。不静默写业务系统。决策 20 / 21、切会话、overlay 收编都不做。

AC 只证明管道通。现网那一屏不好用，先改工作面，再做看板/回滚。

**AE. 应用工作面（先于 AD）**

现网问题（来自 AC 图，不写死某一个应用名）：

1. 一页堆列表、统计卡、新建表单、数据表、连接器、spec 编辑。打开一个应用应该是在用这个台账，不是后台管理台。
2. 「AI 创建」主要在左栏聊天里来回，模块里没有清楚的预览→采纳工作面。
3. 生成出来默认像脚手架：空白登记表压在表格上，看板/统计像规格勾选，不像给人填的台账。
4. 同名草稿一直占列表，和运行中的应用抢视线。

要做成：

- 选中运行中的应用：主区只做这个应用（视图切换 + 当前视图）。列表、草稿、连接器、spec 编辑让开，要找再打开。
- 新建进表单视图或抽屉，不要永远占半屏空表单。
- 创建向导留在应用 Tab：描述 → 预览 → 采纳，不要只丢给左栏会话。
- 草稿能收起来（归档/不置顶），两个同名草稿不能和正在用的应用抢第一眼。
- Builder 默认：表格是主视图，枚举字段用词表/spec 的选项，空状态说人话。不写死台账名字。

**AD 仍后置：** 看板拖卡、改字段、版本回滚。工作面不好看之前做这些，还是脚手架。

请批 **做 AE**、**仍先做 AD**，或 **先停**。

## Ace 补想法（2026-09-19，待批）

打开不要在列表页挤着用；草稿和已激活都要能删；工作台不够时用创建应用补洞、并复用已有能力。

补全和边界写在 [应用打开与能力](apps-open-and-capability.md)。建议下一刀是那里的 **AE′**，不是先做 AD。

## AE 结果（2026-09-19）

[Open apps in surfaces](bc-8ae62cbe-d0dc-5821-a103-92fc9ce25625) 图过。草稿对话框、使用离开列表、删除确认、已激活默认软删。决策 22 未踩坏三 Tab。

## Ace 已批（2026-09-19）

**做 AD。** 一个 worker，grok-4.6 high。同一条已激活应用：看板见到旧行、加字段后多一列、回滚后新列消失且旧行还在。决策 22 不得踩坏 AE/AC 和业务记录。

## AD 已停（2026-09-19）

Ace：右栏拉伸时左栏会话列表应收起/展开，被改坏。AD 停。先修回归。

## 会话列表拉伸回归（2026-09-19，图过）

[Restore session list resize](bc-8a7f0579-5dca-5a20-a100-71bf36f821d7)

- **图过。** 拉宽右栏 → 会话列表收起；拉窄 → 展开。根因：DSH iframe 抢走拖拽指针，右栏宽度到不了收起阈值。
- `main` `2b44ae3`。应用打开/删除入口、三 Tab 还在。AD 没继续。

## Ace 已批（2026-09-19）

**重做 AD。** 拉伸回归图过之后，按原 AD 再做：同一条已激活本地应用，看板见到旧行、加字段后多一列、回滚后新列消失且旧行还在。决策 22 必须复验：拉宽右栏会话列表收起、拉窄展开；不要拆 StagePanel 对 DSH iframe 的指针屏蔽。不恢复第一台 AD。

## AD 结果（2026-09-19，图过）

[Retry kanban field rollback](bc-16301578-a52f-55e9-a5f8-6f1a33d094d2)

- **图过。** 同一条已激活应用「本周现场走访记录」：看板「计划」列见到 `走访-mu7vaswd`；加字段并「激活本次修订」后表头多「附加」，旧行还在；回滚修订 1 后新列从界面消失，旧行还在。SQLite 只增不减，`phone` 列留下、行没删。
- 决策 22：验证文量到拉宽 aside 48px、拉窄 224px；StagePanel / iframe 指针屏蔽未改。应用列表、打开工作面、删除入口、三 Tab 还在；kind chip 不进本地应用名。
- `main` `12aa131`。未推 origin。未静默写业务系统。未写死应用名。
- 图是看板 + 加列。回滚看验证文（回滚后 `current_revision=1`）。这张图两边会话列表都是展开，拉伸收起没进同一张图。
- 规格 04 验收第 4 条「拖卡改状态」这次没做，不在本刀过关范围。

## 样本应用不是产品（2026-09-19 Ace 纠正）

「我的业务应用」交付的是：**用户自己创建应用** + 通用运行时。  
AC/AE/AD 留下的「本周现场走访记录」只是现网验证样本，不是我们在做的那个应用，禁止按「优化这个台账」往下做。

[Prove kanban card drag](bc-e5a38b10-8cc2-5a87-a660-37d99c76c771) 已停。AF 是误读「继续」。

## Ace 已批（2026-09-19）

**把「用户自己创建」做清楚。** 一刀 **AG**。一个 worker，grok-4.6 high。

产品是创建能力，不是某条台账。现网「本周现场走访记录」只当用户已经有的应用，禁止打开去改它、禁止当模板、禁止写进产品文案。

要做成：

1. 应用 Tab 列表是目录。主入口是「创建应用」，空状态说人话：这里没有官方台账，你描述需求，生成你自己的应用。
2. 创建留在应用 Tab：描述 → 预览 spec → 采纳并激活 → 进入**这个新应用**的工作面。不要只丢给左栏聊天。
3. 不写死品类名单，不放走访/拜访/邮件/教务/PPT 模板芯片。

过关（图里必须能读到）：

- 创建入口和向导在应用 Tab，不靠先打开走访记录。
- 用一句**新的**描述走通（不要走访、不要拜访台账）；名字只出现在现网数据，不写进 `src/`。
- 预览之后能采纳并激活，进入这个新应用的工作面。
- 走访那条还在列表里，没被改、没被删。
- 新应用不进业务记录 kind chip。

不要做：看板拖卡、加字段回滚、复用邮箱/IM、整页重写、restyle 别的模块。决策 22：拉伸、三 Tab、打开/删除入口还在。失败就停。不静默写业务系统。

## AG 结果（2026-09-19，图过）

[Ship in-tab app create](bc-c76608d0-e3dc-56d4-abc0-fe67af93102c)

- **图过。** 应用 Tab 目录文案是「没有官方台账 / 创建你自己的应用」。主入口「创建应用」。描述 → 预览 spec → 采纳并激活留在这一页。现网用新描述生成「库存盘点」（只在数据里，没进 `src/`）。走访记录未打开、未改，仍运行中。
- 决策 22：验证文量到拉宽 aside 48px、拉窄 224px；iframe 指针屏蔽未拆。三 Tab、删除入口还在。新应用没进 kind chip。未静默写业务系统。
- `main` `81232bb`。未推 origin。
- 图是向导预览 + 目录，不是激活后的工作面。激活后列表里「库存盘点」为运行中，写在验证数据里。
- 验证过程留下同名草稿「库存盘点」和「设备故障报修」，不是产品模板。列表仍会挤。不是本刀要清的用户数据，未删。

## Ace 已批（2026-09-19）

**继续。** 下一刀 **AH**，还是「用户自己创建」，不是看板拖卡，不优化某条台账。

目录和预览一眼能懂：

1. 运行中的应用在目录主角位置。草稿进「草稿」分组，默认不跟运行中抢第一眼。同名草稿分开列，不当一条。
2. 创建预览若有示例行，必须标明「示例」；不要看起来像已经有业务数据。激活后空表说人话。
3. 不删走访、不删已激活应用。不写死应用名。不新造一条应用来证明（用现有目录即可；要拍预览就打开向导，不要再生成）。

不要做：看板拖卡、加字段回滚、复用邮箱/IM、整页重写。决策 22：拉伸、三 Tab、创建入口、打开/删除还在。失败就停。

[Tidy catalog and preview](bc-64cc7ede-24fe-5335-9c13-146b2a727256) 已停。AH 不是「真应用」那一刀。

## Ace 再确认（2026-09-19）

创建出来的应用要能打开真用、好看，并自动接工作台已有能力。不要 HTML 纪念品。这是 [应用打开与能力](apps-open-and-capability.md) 台阶 1 后半，一直没单独开刀。

## Ace 已批（2026-09-19）

**理解对了，继续处理。** 一刀 **AI**。一个 worker，grok-4.6 high。

目标：用户描述一个工作台没有的需求，创建出来的是**这个需求的产品页**，不是通用后台表。数据刷新还在。该用的平台能力要接上。

要做成：

1. spec / 运行时必须能铺产品页：栏目或导航、卡片栅格、概览数字、记一笔、图、流水。工作面按这个应用的 spec 铺，不要把表/表单/看板/统计堆成管理台。
2. 创建向导（应用 Tab）按需求生成这种 spec，不要永远吐同一套脚手架。
3. 行进 SQLite，新建后刷新还在。
4. spec 能声明 `uses`（ai / files / float / memory / im / briefing / biz）。声明了的，工作面要有真入口：AI 开左栏可见会话，浮窗能撕，文件走文件模块。没接上的能力不要画假按钮。
5. 参照图只用来对齐标准，禁止写成内置「健身」「记账」模板，禁止把品类名写进 `src/`。

过关（图里必须能读到）：

- 用一句**新的**描述走通（不要走访、库存盘点、拜访台账）。
- 打开后是产品页（卡片或记账这种栏目+概览+记录），不是后台宽表。
- 现场记一笔，刷新还在。
- 至少一种已声明的平台能力真能用（AI 或浮窗或文件）。
- 走访那条不改不删。三 Tab、创建入口、删除、拉伸还在。

不要做：抄 WorkBuddy HTML、看板拖卡、整页重写业务记录。失败就停。不静默写业务系统。不杀 pnpm 5174。日常树留 main，不推 origin。

## AI 结果（2026-09-19，图过，未齐）

[Ship product-shaped app create](bc-b6203436-dde6-546c-b69d-0eada14a8a49)

- **图过这一截：** 新描述「家庭药箱」生成产品页：概览数字、记一笔、分类图、流水。记下 `药-mu82l4pf` 刷新还在。接了问 AI、撕浮窗。不是后台宽表。不是健身/记账模板。走访未改。拉伸 48/224。
- `main` `49c8a69`。未推 origin。未静默写。
- **还没齐：** 工作面上仍有编辑 spec / 回滚 / 说明字。没有参照图那种多栏目导航，也没有跟练卡片。观感仍是生成仪表盘，不是那两张参照的完成度。

## Ace 已批（2026-09-19）

**继续。** 下一刀 **AJ**。补齐 AI 没齐的：工作面去掉建造后台；要有多栏目和卡片栅格；观感对齐参照图。不当模板。不改走访。决策 22 复验拉伸。

## AJ 结果（2026-09-19，图过，观感未齐）

[Finish product page quality](bc-e3c27668-b75e-55f6-bb0b-fc07e64a145a)

- **图过这一截：** 家庭药箱日常面不再有编辑 spec / 回滚 / 大段说明，建造进抽屉。有栏目「药箱 / 药品」和两张卡片。记下 `记-mu83ag8l` 刷新还在。问 AI / 浮窗还在。走访未改。拉伸 48/224。
- `main` `49ad21d`。未推 origin。未做成模板。
- **观感未齐：** 卡片是色条加字段，标题还是生成单号。没有参照图那种分组卡片/播放，也没有记账那页的概览+记一笔+图+流水同时在。

## Ace 已批（2026-09-19）

**继续。** 下一刀 **AK**。观感对齐参照：业务名标题、分组卡片或记账整屏（概览+记一笔+图+流水）。通用运行时，不当药箱皮肤。不抄 HTML。不改走访。

## AK 结果（2026-09-19，图过，未齐）

[Match product page look](bc-894f8d42-ac04-5a35-ba3f-8b9337ac4515)

- **图过这一截：** 家庭药箱同一屏是概览、记一笔、分类图、流水。新行标题是「对乙酰氨基酚」，不是单号。刷新还在。建造仍在抽屉。问 AI / 浮窗还在。走访未改。拉伸 48/224。
- `main` `f5ee4da`。未推 origin。未做成模板。
- **未齐：** 旧行标题仍回落到「常备」。没有分组跟练卡片。图和装饰仍低于生活工作台参照。

## Ace 已批（2026-09-19）

**继续。** 下一刀 **AL**。分组卡片：按组铺、业务名标题、行动打开 URL 或文件。通用运行时，不当健身皮肤。记账整屏保持。不改走访。

## AL 结果（2026-09-19，图过，未齐）

[Ship grouped product cards](bc-ad72e771-8154-517d-b860-4e75598a4994)

- **图过这一截：** 新描述「资料卡片板」按来源分组（文档 / 网页 / 备忘），标题是入门手册、周报模板、待办备忘。点打开是真 URL。刷新还在。药箱和走访未改。记账整屏路径还在。拉伸 48/224。
- `main` `e1367c4`。未推 origin。不是健身模板。
- **未齐：** 每组一张卡，密度和装饰低于跟练参照。其余能力还没按声明接完。

## Ace 已批（2026-09-19）

**继续。** 下一刀 **AM**。分组卡片同组横排多张，密度和装饰对齐跟练参照。通用运行时，不抄 HTML，不当皮肤。不新造应用，不改走访。

## AM 结果（2026-09-19，图过，未齐）

[Raise card density look](bc-e1d0beb7-6588-5ac8-b255-550425b23ee6)

- **图过这一截：** 资料卡片板文档一组三张横排，网页一组两张横排。标题是业务名，有说明和打开。刷新 6 张还在。走访未改。拉伸 48/224。未新造应用。
- `main` `a41c630`。未推 origin。
- **未齐：** 说明里还夹着探针字。打开在无头浏览器没接到弹窗（DOM 仍是真链）。装饰仍低于跟练参照。其余能力还没按声明接完。

## Ace 已批（2026-09-19）

**继续。** 下一刀 **AN**。spec 声明的 files / memory / im / briefing / biz 要有真入口，走已有模块。没声明的不画按钮。不静默写。不改走访。

## AN 结果（2026-09-19，未过）

[Wire declared app capabilities](bc-fbd2311f-7c7c-51b1-b775-bfa4da76809a)

- **未过。** 声称 `main` `86ad63b`，验证图/文没进 Context。点「引用文件」进了文件模块。点「起草记忆卡片」打开的是图谱工作室（Failed to fetch），不是起草卡；接口还建了一张已经「已入档」的卡，和「只起草、等人点头」相反。IM / 早报 / 业务闸这轮没点。拉伸没复验。走访未改。未静默写业务系统。
- 不要当过。不要在这台 worker 上补刀。

## Ace 已批（2026-09-19）

**继续。** 重派 **AN**。同一刀，不是 AO。声明了的 files / memory / im / briefing / biz 走已有模块。记忆必须是起草卡片（等人点头），不能打开图谱，也不能自动入档。没声明的不画按钮。不静默写。不改走访。验证图必须写进 Context。

[Retry app capability wiring](bc-bbbf789e-fc38-5059-8485-273aa2a4ad93) 进行中。

## AN 重派结果（2026-09-19，图过，未齐）

[Retry app capability wiring](bc-bbbf789e-fc38-5059-8485-273aa2a4ad93)

- **图过这一截：** 验证图已进 Context。资料卡片板工作面上是起草记忆卡片，正文可见，有「点头入档」，不是图谱工作室。声明按钮都在。走访未改。未静默写。
- 声称 `main` `7898736`。未推 origin。
- **未齐：** 这张只拍到记忆起草，文件 / IM / 早报 / 业务记录点进去的画面不在图里。拉伸没进这张图。卡片说明还夹探针字。入口仍是一排按钮。

## Ace 纠正（2026-09-19，待批）

点应用里的浮窗，现在撕走的是整块「业务应用」。要的是**把当前这个创建的应用单独撕成一个浮窗**。按钮文案「撕出浮窗」改成「浮窗」。

撕出浮窗之后，右侧主台又回到默认页，之前打开的状态丢了。**文件模块已经修好**，其他功能模块还没有。要照文件那套把状态记住，不要另发明。

## AO. 应用自己的浮窗 + 主台记住状态

一个 worker，**grok-4.6 high**。不删文件。不写死应用名。不 restyle 别的模块。不静默写业务系统。不杀 pnpm 5174。

要做成：

1. 工作面上的按钮写「浮窗」，不要「撕出浮窗」。
2. 点「浮窗」= 只把**当前这个已打开的创建应用**撕成独立浮窗（标题用这个应用自己的名字，内容是这个应用的工作面）。
3. 不要把「业务应用」整块（目录 / 应用·业务记录·操作记录）撕走。主台里的业务应用板块还在。
4. 顶栏模块自己的浮窗能力还在。两个应用可以各有各的浮窗，不要写成只认某一个台账。
5. **右侧主台要记住撕之前的状态。** 撕出浮窗、关掉浮窗，主台还停在原来的模块和页（业务应用停在当时那个应用/Tab，记忆停在当时那一屏，早报/计划/IM 同理）。不要掉回默认页。文件已经这样，其他功能模块照文件的做法补齐，不要第二套。
6. **浮窗标题栏的「—」= 回到右侧面板，不要缩小成图标。** 所有模块和应用浮窗同一套。
   - 只收回你点「—」的那一个；其他浮窗还在。
   - 右侧面板改成显示这个模块（创建的应用则回到业务应用里、打开这个应用的工作面）。
   - 刚才右侧在看的东西不毁掉，状态记住；要从顶栏再切回去。
   - 「×」只关这个浮窗，不改右侧面板。
   - 浮窗标题用模块名或应用名，不要写成「浮窗」。

过关（图里必须能读到）：

- 按钮是「浮窗」。
- 浮窗里是这个应用的工作面，不是三 Tab 的业务应用板块。标题是应用名。
- 主台业务应用还在，能回到目录；撕完主台没有跳回默认页。
- 「—」之后浮窗关掉，右侧是这个应用/模块，不是桌面图标。
- 两个浮窗同时开着时，点其中一个的「—」，另一个还在。
- 至少再验一个非文件模块：撕出再看主台，还是撕之前那一屏。
- 走访那条不改不删。三 Tab、创建、删除、记忆起草卡还在。
- 真的拖右侧分栏：拉宽会话列表收起，拉窄展开。

不要做：看板拖卡、新造应用、抄健身/记账模板、整页重写。失败就停。日常树留 main，不推 origin。

请批 **做 AO** 或 **先停**。

## Ace 已批（2026-09-19）

**做 AO。** 一个 worker，grok-4.6 high。应用自己的浮窗、主台记住状态、「—」收回右侧。

[Ship app-only float](bc-ac3a6733-203f-5f14-9465-7c65ac18e13e) 进行中。

## AO 结果（2026-09-19，图过，未齐）

[Ship app-only float](bc-ac3a6733-203f-5f14-9465-7c65ac18e13e)

- **图过这一截：** 按钮是「浮窗」。浮窗标题是「资料卡片板」，里面是这个应用的工作面，主台仍是业务应用三 Tab。点「—」收回右侧，不是桌面图标。两个浮窗时只收点的那一个，家庭药箱还在。走访未改。拉伸 48/224 写在验证文。
- `main` `d6d71a5`。未推 origin。未新造应用。未静默写。
- **未齐：** 记忆模块撕完记状态这次没验上。卡片说明还夹探针字。入口仍是一排按钮。

## Ace 纠正（2026-09-19，待批）

业务应用撕出浮窗、收回浮窗时，会先闪一帧「业务记录」：正在检查事务底座 / 先在设置登记业务连接器，再跳回原来的界面。不够丝滑。

## AP. 撕出收回不要闪默认页

一个 worker，**grok-4.6 high**。不删文件。不写死应用名。不 restyle 业务记录。不静默写。不杀 pnpm 5174。

要做成：

1. 业务应用撕出浮窗、点「—」收回，都不要先画业务记录的检查/空连接器页再恢复。
2. 第一帧就是记住的 Tab 和当时打开的应用。人本来就在业务记录，才画业务记录。
3. 不要用遮罩把闪屏挡掉，也不要整页重挂一次再跳 Tab。照文件模块已经记住状态的做法，挂上之前就把页选对。
4. AO 已过的：应用自己的浮窗、按钮「浮窗」、「—」收回、两窗只收一个，不要踩坏。

过关：

- 撕出和收回各走一遍，不能出现图上那帧「正在检查事务底座 / 先在设置登记业务连接器」，除非人当时就在业务记录。
- 收回后仍是原来的应用工作面。
- 走访不改。拉伸 48/224。三 Tab、创建、删除还在。

失败就停。日常树留 main，不推 origin。

请批 **做 AP** 或 **先停**。

## Ace 已批（2026-09-19）

**做 AP。** 一个 worker，grok-4.6 high。撕出收回不要闪业务记录默认页。

[Stop biz float flash](bc-dfca177b-751d-5b3e-bd35-0a8e1e45dc30) 进行中。

## AP 结果（2026-09-19，图过）

[Stop biz float flash](bc-dfca177b-751d-5b3e-bd35-0a8e1e45dc30)

- **图过。** 四格都是资料卡片板工作面，没有「正在检查事务底座 / 先登记连接器」。撕出瞬间和收回瞬间观察器命中 0。收回后仍是这个应用。AO 按钮「浮窗」、应用浮窗、「—」收回、两窗只收一个还在。走访未改。拉伸 48/224。
- `main` `b38b1c3`。未推 origin。未静默写。
- 没闪靠点击后立刻截图 + 观察器，不是人眼连拍。创建应用本身还没齐（探针字、按钮条、记忆记状态）。

## Ace 已批（2026-09-19）

**继续处理下一项。** 下一刀 **AQ**：AO 没验上的模块记状态。

## AQ. 模块撕出收回记住页

一个 worker，**grok-4.6 high**。不删文件。不写死模块名。不 restyle。不静默写。不杀 pnpm 5174。不改走访。不新造应用。

要做成：

1. 记忆撕出浮窗、点「—」收回，右侧仍是撕之前那一屏，不要掉回默认/图谱首页。
2. 早报 / 计划 / IM 若还掉默认页，照文件那套一并补，不要只写死记忆。
3. 不要闪默认页。不要第二套状态机。
4. 不要踩坏 AO/AP：应用自己的浮窗、按钮「浮窗」、「—」收回、两窗只收一个、业务应用不闪连接器空页、拉伸 48/224、iframe 指针屏蔽。

过关（图里必须能读到）：

- 打开记忆某一屏 → 撕出 → 主台还是那一屏或收回后还是那一屏，不是默认图谱首页。
- 再验一个非文件、非业务应用模块同样还在。
- 走访未改。三 Tab、创建、删除还在。

失败就停。日常树留 main，不推 origin。

[Restore module float state](bc-829465ca-41a2-5eaf-a50a-5a19f0a02b75) 进行中。

## AQ 结果（2026-09-19，图过，未齐）

[Restore module float state](bc-829465ca-41a2-5eaf-a50a-5a19f0a02b75)

- **图过这一截：** 记忆停在导入导出，不是图谱首页；撕出和「—」收回都还在这一屏。另验计划·日程，收回后仍是日程日视图。走访未改。拉伸 48/224。AO/AP 按钮「浮窗」、两窗只收一个、连接器空页未闪。
- `main` `dc0ea9c`。未推 origin。未静默写。
- **未齐：** IM 这轮没点。早报记住了 settings=open，抽屉标题现网没扫到。创建应用本身还没齐。

## Ace 已批（2026-09-19）

**继续。** 下一刀 **AR**：补 AQ 没点的 IM。

## AR. IM 撕出收回记住会话

一个 worker，**grok-4.6 high**。不删文件。不自动发 IM。不静默写。不杀 pnpm 5174。不改走访。不新造应用。

要做成：

1. IM 撕出浮窗、点「—」收回，仍是撕之前那个会话和输入框，不要掉回默认空白。
2. 照文件 / 记忆 / 计划已经记住状态的做法，不要第二套。
3. 不要踩坏 AO/AP/AQ。

过关：图里同一个 IM 会话，不是默认空白。走访未改。拉伸 48/224。失败就停。日常树留 main，不推 origin。

[Remember IM float state](bc-e84f3e6a-316e-56f1-ba2d-c803cdc05cc4) 进行中。

## AR 结果（2026-09-19，图过）

[Remember IM float state](bc-e84f3e6a-316e-56f1-ba2d-c803cdc05cc4)

- **图过。** IM 撕出和「—」收回都停在同一会话，输入框稿还在，没有发出去。不是默认空白。AO/AP/AQ 未踩坏。走访未改。拉伸 48/224。
- `main` `3d038a1`。未推 origin。未静默写。未自动发 IM。
- 早报抽屉标题仍没验。创建应用本身还没齐。

## Ace 已批（2026-09-19）

**继续。** 下一刀 **AS**：回到创建应用。产品页按自己的栏目铺，能力仍是真入口，不要永远一排按钮；说明不要夹探针字。

## AS. 产品页不要工具条和探针字

一个 worker，**grok-4.6 high**。不删文件。不写死应用名。不抄健身/记账模板。不改走访。不新造应用。不静默写。不杀 pnpm 5174。

要做成：

1. 工作面按这个应用自己的栏目铺。声明了的平台能力仍能点到已有模块，没声明的不画。不要永远在产品页顶上压一排「问 AI / 浮窗 / 引用文件 / …」。
2. 卡片说明只用来自 spec 的业务字段，不要把验证探针字显示成说明。不要写死某一串探针。
3. 不要踩坏 AO–AR。

过关：图里是产品页不是工具条；说明是业务文案。走访未改。拉伸 48/224。失败就停。日常树留 main，不推 origin。

[Tidy app capability chrome](bc-8b880fb7-1d38-5008-b0bc-dccf832ae94c) 进行中。

## AS 结果（2026-09-19，图过，未齐）

[Tidy app capability chrome](bc-8b880fb7-1d38-5008-b0bc-dccf832ae94c)

- **图过这一截：** 产品页顶是栏目「资料板」，不是一排「问 AI / 浮窗 / 引用文件」。卡片说明是业务文案（字段和错误码、每周五更新等），没有探针 token。工作面有「浮窗」。走访未改。拉伸 48/224。
- `main` `b298fba`。未推 origin。未新造应用。未静默写。
- **未齐：** 这张没拍到记下栏的其余能力入口，「问 AI」这张看不见。左栏旧会话还留着当时的探针字。创建应用观感还低于参照。

## Ace 已批（2026-09-19）

**继续。** 下一刀 **AT**：声明了的能力在产品页里要找得到。问 AI 开左栏可见会话。不要又回到顶上一排工具条。

## AT. 声明能力入口找得到

一个 worker，**grok-4.6 high**。不删文件。不写死应用名。不抄模板。不改走访。不新造应用。不静默写。不杀 pnpm 5174。

要做成：

1. spec 声明了的每条能力，工作面上找得到、点到已有模块。问 AI 开左栏可见会话。没声明的不画。
2. 不要把能力又排回栏目顶。不要只藏建造抽屉。
3. 不要踩坏 AO–AS。

过关：图里能看见这些入口，栏目顶仍不是工具条。走访未改。拉伸 48/224。失败就停。日常树留 main，不推 origin。

[Show declared capability entries](bc-483e62cb-051b-5218-b864-63a8e53e7df7) 进行中。

## AT 结果（2026-09-19，图过，未齐）

[Show declared capability entries](bc-483e62cb-051b-5218-b864-63a8e53e7df7)

- **图过这一截：** 栏目顶仍是「资料板」。记下栏能看见问 AI / 引用文件 / 起草记忆卡片 / 拟回 / 早报 / 业务记录。左栏有「资料卡片板 · 问」。工作面有「浮窗」。走访未改。拉伸 48/224。
- `main` `d5e67b4`。未推 origin。未静默写。
- **未齐：** 入口仍是一排按钮，只是挪到记下栏下面。这张上半截还有「事务底座运行正常」。创建应用观感还低于参照。

## Ace 已批（2026-09-19）

**继续。** 下一刀 **AU**：能力跟这个应用自己的记录动作放，不要记下栏再铺一排按钮；工作面不要叠业务记录「事务底座」条。

## AU. 能力跟记录动作走

一个 worker，**grok-4.6 high**。不删文件。不写死应用名。不抄模板。不改走访。不新造应用。不静默写。不杀 pnpm 5174。

要做成：

1. 应用工作面不要出现业务记录的「事务底座运行正常 / 正在检查 / 先登记连接器」。人切到业务记录才画。
2. 声明能力仍找得到、点到已有模块。问 AI 开左栏可见会话。不要栏目顶工具条，也不要记下栏再铺一排同款按钮。跟记一笔 / 添加资料 / 打开这些已有动作放在一起。没声明的不画。不要只藏建造抽屉。
3. 不要踩坏 AO–AT。

过关：图里没有事务底座条，没有记下栏一排平台按钮，声明入口仍找得到。走访未改。拉伸 48/224。失败就停。日常树留 main，不推 origin。

[Fold app capability actions](bc-2ae88857-fc83-5978-a1e8-55632ffe2c16) 进行中。

## AU 结果（2026-09-19，图过，未齐）

[Fold app capability actions](bc-2ae88857-fc83-5978-a1e8-55632ffe2c16)

- **图过这一截：** 工作面没有「事务底座运行正常」。问 AI / 文件 / 记忆 / IM / 早报 / 业务记录和「添加资料」同一行，记下栏不再另铺一排。栏目顶仍是「资料板」。走访未改。拉伸 48/224。
- `main` `4c3fcc1`。未推 origin。未静默写。
- **未齐：** 平台能力仍是一排按钮，只是跟添加资料并排。创建应用观感还低于参照。

## 创建应用还没齐（2026-09-19）

已经能用的不列。下面只剩创建应用本身。

还要做：

1. **按需求铺出不同产品页** — 教务和运营不能是同一套脚手架换列名。向导要按对象/字段/动作/视图生成栏目、卡片、概览、记一笔、图、流水，不要永远吐同一张皮。
2. **观感对齐参照** — [健身工作台](/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/media/workbuddy-fitness-ref.png)、[生活工作台·记账](/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/media/workbuddy-life-ref.png)。跟练卡片、播放、密度和装饰还低于这两张。不当模板、不抄 HTML。
3. **能力长进应用自己的动作** — 现在问 AI / 文件 / 记忆 / IM / 早报 / 业务记录还是一排按钮，只是跟「添加资料」并排。要跟记一笔 / 打开这些动作长在一起，不要第二套工具条。
4. **向导先问清接什么** — 本地台账，还是邮箱 / 业务系统 / 文件。接就走已有模块，不编假接口，也不默认铺满所有 uses。
5. **计划「摘成待办」** — 声明了就要真落库，现在创建应用还没接。
6. **旧行标题回落** — 没有业务名时还会落到分类值（药箱旧行「常备」是例子，不要写死）。
7. **「打开」要真打开** — 链接是真的，无头浏览器弹窗没证过。
8. **目录里草稿还挤** — 同名草稿和运行中抢第一眼。AH 当时停了，目录分层还没做。

本版不做：L2 代码应用、课表日历、假 Gmail/PPT/店后台、一次写多张业务表、跨业务系统。看板拖卡不按样本应用去优化。

小尾巴（不算创建应用主线）：早报浮窗抽屉标题、左栏旧会话里的探针字。

## Ace 已批（2026-09-19）

**一次做完。** 上面 8 项合成一刀 **AV**。不要另发明第 9 条口号。

## AV. 创建应用八项一次做完

一个 worker，**grok-4.6 high**，Ace 本机。不删文件。不写死应用名。不抄模板。不改走访。不静默写。不杀 pnpm 5174。

八项都必须现网做成：

1. 向导按对象/字段/动作/视图铺不同产品页。两句**新的**不同描述，教务 ≠ 运营换列名。
2. 观感对齐参照，不当模板、不抄 HTML。
3. 声明能力长进这个应用自己的动作，不要第二套工具条。
4. 向导先问清本地台账还是邮箱 / 业务 / 文件。接就走已有模块，不编假接口，不默认铺满 uses。
5. 声明了「摘成待办」必须真落库。
6. 没有业务名标题时不要回落到分类值。
7. 「打开」是真 URL/文件。
8. 目录：运行中主角，草稿分组让开。同名草稿分开列。不删走访或已激活应用。

过关：八项逐条过。失败就停，不要自称过。不要踩坏 AO–AU。日常树留 main，不推 origin。

结果写到 [verify-app-create-av.md](/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/internal/verify-app-create-av.md) 和 [app-create-av.png](/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/media/app-create-av.png)。

[Finish remaining create-app gaps](bc-564b3342-8335-5dca-8acd-3b7c98ff40e6) 进行中。

## AV 结果（2026-09-19，图过，未齐）

[Finish remaining create-app gaps](bc-564b3342-8335-5dca-8acd-3b7c98ff40e6)

- **图过这一截：** 两句新描述走出两套页：菜谱卡片+播放 vs 抄表概览/记一笔/图/流水。向导默认本地，模块勾选全关。记录动作是问 AI / 播放 / 摘成待办，没有平台按钮条。标题是番茄炒蛋 / 东门水表。目录运行中在前，走访还在。拉伸 48/224。走访 `updatedAt` 未改。
- `main` `b686f9b`。未推 origin。未静默写。
- **未齐：** 观感密度和装饰还低于参照。摘成待办只见到接口 1→2，图没拍计划页。「打开」href 是真链，弹窗没截到。

## Ace 已批（2026-09-19）

**把未齐的部分补齐。** 下一刀 **AW**：只补上面三条尾巴，不要另发明。

## AW. 补齐 AV 未齐

一个 worker，**grok-4.6 high**。不删文件。不写死应用名。不抄模板。不改走访。不新造第三套应用。不静默写。不杀 pnpm 5174。

要做成：

1. 观感密度和装饰对齐参照（跟练/播放卡、数字、记一笔、图、流水）。不当模板、不抄 HTML。
2. 「摘成待办」要在计划页看得见那条，不只是接口计数。
3. 「打开」/播放要点一次并证窗口或跳转发生。证不到就停。

不要踩坏 AO–AV。拉伸 48/224。日常树留 main，不推 origin。

[Close leftover create-app tails](bc-60d13af3-4103-5cbf-ad7d-a5263ed6532f) 进行中。

## AW 结果（2026-09-19，图过，未齐）

[Close leftover create-app tails](bc-60d13af3-4103-5cbf-ad7d-a5263ed6532f)

- **图过这一截：** 计划页选中「小区水电抄表」。点播放后窗口打开 `https://example.com/watch?v=aw-ribs`（隧道连不上，错误页上 URL 对得上）。菜谱三卡横排、标题压在色块上。抄表同一屏仍是概览/记一笔/图/流水。走访未改。拉伸 48/224。
- `main` `ffa4767`。未推 origin。未静默写。未新造第三套应用。
- **未齐：** 观感比上一刀密，密度和装饰仍低于参照。

## Ace 已批（2026-09-19）

**继续把未齐补齐，URL 打不开也要处理。** 下一刀 **AX**。

## AX. 观感 + 真打开 URL

一个 worker，**grok-4.6 high**。不删文件。不写死应用名。不抄模板。不改走访。不静默写。不杀 pnpm 5174。

要做成：

1. 观感密度和装饰再对齐参照。不要只加色块就自称齐。
2. 自创应用里的打开/播放要真打得开。不要用 example.com 当过关。找出通用原因并修。证一次现网能打开的链。

不要踩坏 AO–AW。拉伸 48/224。日常树留 main，不推 origin。

[Fix look and app URLs](bc-40c518ac-b8d6-5e55-a7df-42fe0d401c41) 进行中。

## AX 结果（2026-09-19，图过，未齐）

[Fix look and app URLs](bc-40c518ac-b8d6-5e55-a7df-42fe0d401c41)

- **图过这一截：** 点播放弹出页正文是「AX打开证明」，地址 `http://127.0.0.1:48721/ax-open`，不是错误页。菜谱卡中心播放圆、标题在色块下；抄表同一屏概览/记一笔/图/流水。走访还在。拉伸 48/224。走访 `updatedAt` 未改。
- `main` `209e426`。未推 origin。未静默写。未新造应用。
- **未齐：** 观感又密了一档，仍低于参照。打包 Electron 窗体没证。夹具链后来改到未进 git 的 `ax-open.html`，证完可能又是死链。

## Ace 已批（2026-09-19）

**一次做好观感。** 下一刀 **AY**。只做工作面观感。再加色块/按钮高度不算过。

## AY. 观感一次对齐参照

一个 worker，**grok-4.6 high**。不删文件。不写死应用名。不抄模板。不改走访。不静默写。不杀 pnpm 5174。

过关：卡片页接近健身参照密度（播放是主操作，一组多卡挤满一行）；台账页接近记账参照密度（概览/记一笔/图/流水同一屏挤满）。对照 AX 图，同一套布局只改圆和按钮高度=失败。只改生成工作面，不改别的模块皮。

[Finish app look in one pass](bc-468a8cb8-dcca-53f1-96f5-f1443de076ce) 进行中。

## AY 结果（2026-09-19，图过，未齐）

[Finish app look in one pass](bc-468a8cb8-dcca-53f1-96f5-f1443de076ce)

- **图过这一截：** 对照 AX，布局换了：标题压进色块，播放是通栏主按钮，不再是中心圆+双芯片。台账记一笔改成胶囊，概览/记一笔/图/流水同一屏。走访还在。拉伸 48/224。走访 `updatedAt` 未改。
- `main` `4df6629`。未推 origin。未静默写。
- **未齐：** 跟参照比，卡片还是大色块留白；粤菜单张空一截；流水还是薄一行，没有参照那种满屏信息流。

## Ace 已批（2026-09-19）

**做。** 接完同一条路。别做成半成品。别改坏现有功能。下一刀 **AZ**。

## AZ. builder 把创建和改写进 spec

一个 worker，**grok-4.6 high**。不推翻。不删文件。不写死应用名。不抄模板。不改走访。不静默写。不杀 pnpm 5174。

三条必须同时现网，少一条就算半成品：

1. spec 写得下工作面怎么铺。写不下就扩。
2. 新建：向导一句新描述，builder 写进 spec 并画出来。
3. 改已有：对夹具再说一句，同一 builder，预览后出新修订并画出来。必须是 spec 变了，不是改共用 CSS。

决策 22：三 Tab、创建/删除、目录、摘成待办、打开、问 AI、拉伸 48/224、走访不动。

[Finish builder spec revise path](bc-74ab66a4-c9e1-5ba4-b947-c7e73ef89595) 进行中。

## AZ 结果（2026-09-19，图过）

[Finish builder spec revise path](bc-74ab66a4-c9e1-5ba4-b947-c7e73ef89595)

- **图过三条：** spec 有 surface（density / hero / primary）。新建「阳台植物浇水」按 packed、打开做主操作画出来。家里的菜谱修订 1→3：大色块标题压上变成小卡标题在下，是 spec.surface 变了。走访还在。目录运行中在前。
- `main` `cb694ad`。未推 origin。未静默写。
- **问 AI：** 验证图没点中。Ace 现网点「问 AI」能在左栏新建会话并跑起来，这条算过。

## Ace 已批（2026-09-19）

**先把业务记录整块做收口。** 创建应用主线不动。收口方案在 [业务记录对齐方案](/cursor/stores/bc-aea46619-2327-43a7-a094-57e6e76a7d7e/docs/records-align-plan.md) 的 BA。不是评测集，不是第 9 步，不是 20/21。

[Close records daily loop](bc-6e02db5e-a056-5087-b1cc-bac85cb3338b) 没过：第 3 条右表没换，4–10 没跑。

**继续 BB。** [Retry records close-out](bc-85c1f888-3d9f-5f35-8404-b5e41069c6b1) 没过：1–3 过，第 4 条改行抽屉没开，代码在旁支。

**继续 BC。** [Retry records close after 恒通](bc-59d2d612-ea68-552f-a3e2-49292cb116f4) 进行中。接到 main，第 4 条要改行预览。
